[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "taxi-env"
version = "0.1.0"
description = "Taxi OpenEnv environment"
authors = [{ name = "Mohammed Affanulla" }]
readme = "README.md"
requires-python = ">=3.8"

[project.dependencies]
gym = "*"
numpy = "*"